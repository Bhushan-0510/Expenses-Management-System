# Example environment variables for frontend
# Use your deployed backend (Render) URL:
VITE_API_URL=https://expenses-management-system-ga8g.onrender.com/api

# Or use a local backend (uncomment to use):
# VITE_API_URL=http://localhost:5000/api

# After changing this file, restart the Vite dev server so the env is picked up.
